// How to Play Online — a static guide for the posse.
import { injectDefs, mountNav } from './common.js';
import { mountTableLog } from './tablelog.js';

injectDefs();
mountTableLog();
mountNav('/howto');
