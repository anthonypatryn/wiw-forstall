Drop real recordings here as <name>.mp3 and add the name to manifest.json to replace the synthesized sound.
Names: dice, card, gun, bow, swing, explosion, forstall, zap, lockClick, lockSnap, lockOpen, success, fail, chime.
