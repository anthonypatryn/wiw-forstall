# WIW Table Kit: working notes

## Source material
- Guidebook PDF: `C:\Users\ampat\Downloads\WIW-Guidebook-Digital-Page.pdf`.
  - It is 276 pages and over 100 MB, so read it with PyMuPDF text extraction. The Read tool can't open it.
  - **printed page = PDF page − 14**.
- Trade sheet PDFs and the 5 battle maps came from `Downloads`.
- Book pages used so far:
  - pp. 6–8: character creation
  - p. 32: spend Prestige (pcOp `spend`); p. 54: Jackpot + Bleeding Out (pcOp `bleedRoll`); p. 58: High Noon Duel (`state.duel`, duelAction, sides `pc:<id>`/`en:<id>`)
  - p. 34: Achievements (`ACHIEVEMENTS` in sheets.js; Warden action `achieve`); pp. 52/54: campfire vs town rest (pcOps `campRest`/`townRest`, Warden `townRestAll`)
  - p. 79: trophies & loot (`lib/trophies.js`, all 59 bestiary trophies; Warden actions `loot`/`search`); pp. 93–100: upgrades (`upgradeFits`/`upgradeType`, pcOps `installUpgrade`/`removeUpgrade`), p. 76: Special Ammo (pcOp `ammo`); pp. 104–108: horse Bond/breaking (pcOp `breakHorse`); p. 92/94: mech Condition from Health (`mechState`), repairs (pcOp `mechRepair`)
  - pp. 41–43, 49: combat helpers — pcOps `attack` (weapon Grit, Aim reroll, Special Ammo fx, auto target Defense), `dodge` (banked `pc.dodge`, cleared in startTurn), `relieve` (1 Grit/die, once per Status per turn), `endTurn`; Warden `enemyAttack` (damage vs Defense+Cover+Dodge, Piercing, statuses). Players act only on the Battle Map turn panel; the sheet's `renderFight` panel now shows just Warden roll requests + out-of-combat Status relief.
  - pp. 12–13: Skill checks — Warden `checkStart`/`checkClose`, called with the tap-to-call panel `mountRollCaller` (public/js/rollcall.js + css/rollcall.css: who chips → difficulty chips → tap a Skill to send; on Run the Game), pcOp `checkRoll` (Helping = half dice, best helper added), `state.checks` in the player view, prompt on the sheet's fight panel + HUD pop-up on every page; players not called get a "Want to help?" HUD pop-up (hud check `helped` list, `wiw.help.<id>` = dismissed). Health never exceeds Max (changeHealth + setSheetField); rests refill Grit.
  - p. 13 Challenge rolls (check `kind:'challenge'`, NPC side via duelist refs, ties reroll). p. 34: `autoAchievements` (The Scrapper at 100 Scrap) runs after every /api/combat POST. Backup: `lib/routes/backup.js` (Warden) dumps all Redis docs; Run the Game download button.
  - Grit engine (pp. 40–43): every Action spends Grit via `spendGrit` and is noted in `actor.turnLog` (reset in startTurn). Map moves: lib/routes/battle.js calls `chargeMove` (speed: Fast/Normal/Slow/Very Slow, rough ×2, mounted horse = Fast, mech = its speed, Compromised ×2 max 6) and saves the combat doc; `undoMove` refunds. `autoSync` in lib/battle.js keeps tokens in step with the fight. Turn panel = `renderTurnBar` in public/js/battle.js.
  - Trade powers in play: `abilityInfo` parses "Spend N Grit" / "roll XG" / "(2/day)" from each ability; pcOp `useAbility` spends, rolls, ticks `abilityUses` (Town rest resets). Grit changers: Fired Up (+3 now, `gritNext`=3, can't use while `gritLimited`), Fired Up 2 (separate; 2 allies `gritNext`=8, Marshal 2, 2/day +2 Health), Crippling Precision (`crippledNext` → moves ×2), Biological Amplification (free Aim/Dodge next turn), Arabian Revered +1 Grit 2/day (`horseGrit`), Digging Deep heals over Max. Picker = `abilityOptions` in common.js (map turn panel; battle.js redraws the panel once `?view=meta` loads).
  - Who's in the fight: `start {posse:[ids], enemies:[ids]}` (from `pickFighters` in common.js — tap chips, dark = in; Battle Map, Combat Control, Run the Game) sets `combat.party` and `e.out`; `inFight()` gates turn order, initiative and Battle Map auto-tokens. Mid-fight: `join {id}` (rolls Finesse if needed, re-orders) and `leave {id}`; `end` clears both. Run the Game has Join / out buttons.
  - Undo in combat: lib/routes/combat.js snapshots the fight (`pushUndo`, `state.undoStack`, max 12, cleared on start/end) before each `isUndoable` action; `{action:'undo', mode:'last'|'turn'}` → `undoCombat` restores it, trims the log and puts map tokens back (move snapshots carry `token`). Players can only undo within the current turn; the Warden can go further. Views expose `undo: {last, lastIsThisTurn, thisTurn}`.
  - HUD on every page (`renderHud` in tablelog.js, fed by `logView().hud`): turn-order strip (collapsible, `wiw.hudClosed`), start-of-combat roll pop-up (`combat.startInfo`, seen once per tab), Skill-check pop-up for the device's "This is me" character (skipped on their own sheet), and the sticky "your turn" bar (`myTurn`, every page but /battle: Go to Battle Map / End my turn / × hides till next turn; sets `--turnbar-h` so `.sitenav`, `.warden-strip` and `.sheet-bar` stick below it). Combat Control mounts it via `mountHud()`.
  - Prepare (p. 42): pcOp `prepare {hold:{kind attack|dodge|item|ability|improvise, …, trigger{type within|moves|attacks|allyAttacked|custom}}}` pays now → `pc.hold`; `fireHold` runs it with `prepaid` (no Grit); `dropHold`; fizzles in startTurn. `checkHoldTriggers` runs on enemy map moves (lib/routes/battle.js, hex distance) and enemy attacks; HUD pop-up for your triggered hold; watch-icon token badge.
  - Range from the map: lib/routes/combat.js loads the battle doc for attacks/fireHold/enemyAttack and `applyMapRange` sets `body.range` to the real band (hex distance) or throws; enemy attacks throw `OUT_OF_RANGE:` which the Warden can `force` after a confirm. Without both tokens on the board, the chosen range stands.
  - Forstalls on the map (pp. 81–87): `lib/forstall.js` (KINDS Backpack/Saddlebag Short 6″, Mech Long 18″, Town = whole map; `fields(battle, combat)` lists every Forstall in play; `sweepTolerance` parses "Sweep [N]" + Frenzy text; `bestSweep`, `edisonConflicts`). A character's Forstall comes from their sheet and rides on their pc token (key `pc:<id>`); the Warden's free-standing ones are `battle.forstalls` (actions addForstall/moveForstall/editForstall/removeForstall, `battle.cave` = Rule 2, auto-on for Monster Burrow). Active Sweeps live in `combat.sweeps[key]` (in undo snapshots). Combat action `{action:'forstall', op: sweep|off|burst|edison, key}` → `forstallAction` (Grit on own turn, 1 charge, `EDISON:` error unless `force` → Rule 1). `sweepHit` runs in lib/routes/combat.js when a monster's turn starts and in lib/routes/battle.js when an enemy moves into Range (cries out). Known = only the Forstall's 4 memory slots ("Name · kz"; sheet `forstall.kz.N` is a drop-down of Scanner-decoded monsters, editable in view mode; the Warden's slots can hold any monster). Enemy op `submerged`. Scan rolls (`whoId`) are refused out of the roller's Forstall Range when both are on the map.
  - Forstall extras: Natural EMP (forstall op `emp`, Warden; worm's `empUses` ≤2/day, reset by a Town Rest, `state.emp` blocks Scan/Burst for those Forstalls until the worm's next turn, cleared in lib/routes/combat.js), Heartbeat Sensor (any `heartbeat` upgrade → battle view `pulse {count, nearest}` within Range + 6″ while Sweeping), Mechanic Forstall Efficiency (sweep `efficiency:true`, 2/day, one Hit → Ace), one Scan per round (`combat.scanRound`, lib/routes/scan.js).
  - p. 33: Prestige tiers + Starting at Higher Prestige loadouts (`TIERS` in lib/sheets.js, pcOp `tierKit`)
  - p. 85: ranges
  - pp. 83–84: scanning
  - pp. 120–133: factions
  - pp. 141–188: monsters
  - p. 191: human enemies
  - p. 204: NPC names

## Architecture
- Static pages live in `public/` (plain ES modules, no build step). **One Vercel function** (Hobby plan limit is 12): `api/[area].js` routes `/api/<area>` to the handler in `lib/routes/<area>.js` (logic in `lib/<area>.js`). A new area = a new `lib/routes/<area>.js` + one import/entry in `api/[area].js`. dev-server.mjs uses the same router.
- `lib/store.js`:
  - `load(key)` / `save(state, key)` go to Upstash Redis.
  - Locally they use `.data/<key>.json` instead. Delete `.data` before committing; it is test data.
- `lib/http.js`:
  - The PIN comes from the `x-warden-pin` header.
  - Locally the PIN defaults to `1234`. In production it is the `WARDEN_PIN` env var, currently **1327**.
- Clients poll with `startPolling(view, onState, onConn, endpoint)` and `?since=v`. Composite versions look like `${a.v}.${b.v}`.
- Warden mode:
  - The PIN is kept in `sessionStorage` key `wiw.pin`, per tab.
  - Helpers in `public/js/common.js`: `tryWarden`, `forgetWarden`, `wardenModal`, `markWarden`.
  - Warden mode shows in the nav (red rule, star items, **Needs you (n)**, **Warden ▾** with Switch to player view); `markWarden()` just redraws the nav. The masthead `#warden-btn` is hidden but kept: the nav's **Warden** button clicks it so each page runs its own unlock + reconnect (pages without one use `wardenModal` + reload).
  - It sticks across pages: with a PIN, `/` (player.js) redirects to `/run`. The nav is grouped (`NAV_PLAYER` / `NAV_WARDEN` in common.js; see STYLEGUIDE.md → Navigation); phones get a Menu sheet.
  - **Run the Game** `/run` (public/run.html, js/run.js, css/run.css) is the Warden's home: first in the Warden nav, and `/` (player.js) redirects a Warden there. One page with Needs you (store requests get inline Approve/Deny via /api/shop `decide`), the fight (turn, order, Next turn, Start/End combat), enemies + posse rows with Health ± and Grit, and a Jump To grid. Reuses the combat warden view + `?view=needs`.
  - **Needs you** (nav badge, every page): `GET /api/combat?view=needs` → `wardenNeeds` (pending store requests, open Skill checks, triggered holds, Bleeding Out, Edison clashes, whose turn), polled every 6 s by `pollNeeds` in common.js; the dropdown also links Run the Game / Combat Control / Battle Map / Store / NPCs.
  - `/combat` = **Combat Control**, Warden-only (gate card for players; not in the player nav). Players fight from the Battle Map + sheets.
  - NPC ledger: players see only the ledger (deal/table-felt hidden) and the server allows only `posseNote`/`where` without the PIN (`add` is Warden-only).
- Dice:
  - `lib/dice.js`: B = [blank, blank, spur, hit, hit, ace]; G = [blank, spur, hit, hit, hit, ace]. An Ace counts as 2 Hits.
  - Spurs are rerolled when the roller has the matching Talent.
  - Animation: `animateRoll(tray, dice)` in common.js. `rollPopup(r, title)` shows it for rolls on pages that have no tray of their own.
- Dice inputs are always **Black/Gold number boxes** (`poolHTML` / `readPool` / `fillPool`). Never ask the user to type "3B1G".
- Character sheets:
  - `lib/sheets.js` defines `newSheet`, `PACKS`, `KEEPSAKES`, `giveStartingWeapons`, `weaponFields`, and a whitelisted `RULES` validator.
  - Every new sheet field needs a RULES entry.
  - Multi-field saves send `{action:'sheet', id, fields:{path:value}}`.
  - **Table view** (sheet bar button, `wiw.tableView`, default on phones ≤700px, only while viewing a finished sheet): `.table-view` on #sheet-view hides story/prestige/inventory sections and pickers, and reorders Health → Statuses → Weapons → Gear → Forstall/Horse/Mech → Skills → Abilities with CSS `order` (`.sheet` becomes `display: contents`).
  - View/edit modes: `done:false` = being created (checklist + Save character → pcOp `finish`). Finished sheets (`done` true or missing) open locked; `applyMode` in posse.js disables fields except `PLAY_PATHS` (wallet/scrap/supplies, horse/mech health, ammo) and the Health/Grit/Status/uses buttons. `#id/edit` opens in edit mode.
- Store → sheet: `equipItem(pc, item, qty)` in lib/sheets.js places every bought/given item (lib/shop.js `decide`/`give`): weapons → a free weapon slot, Forstall models → `pc.forstall`, mechs, horses, Special Ammo → a matching weapon's ammo slot (arrowheads = bows), upgrades → `attachUpgrade` (shared with pcOp installUpgrade), gear/traps/crystals → gear slots; goods & services stay in `pc.items` only. No room → `{warning}` shown with `tell()` (OK-only dialog) and logged; the item stays in inventory. The sheet's store-list pickers send pcOp `pick {kind, i, itemId}` (lib/routes/combat.js looks the item up, custom ones included) and the server fills the section with the same `weaponFields`/`gearFields`/`forstallFields`/`mechFields`/`horseFields`. Sheet inventory rows show **Put on sheet** (shop action `equip`, players allowed) for anything not placed yet. The sheet sections are the record and `pc.items` mirrors them: `placedIn(pc, entry)` (sheets.js) says where each row sits ('Weapons', 'Forstall'…; '' = no section; null = not placed), added as `items[].placed` in the player/Warden combat views and the shop view; pcOp `pick` also adds the picked item to the inventory and drops the one it replaced. Removal is linked both ways: the section ✕ (pcOp `removeThing`) also drops the inventory entry, and the inventory ✕ (pcOp `dropItem`) also takes it off the sheet (`unequipItem`). Horse/mech pictures only show while the breed/class is filled in.
- **Selling anything:** `sellables(pc, shop)` (lib/shop.js) = every inventory row (`inv:<uid>`) plus sheet things with no inventory row: starting/hand-filled weapons (`weapon:<i>`, matched against inventory counts per itemId), gear (`gear:<i>`), `forstall`, `horse`, `mech`; suggested price = half the catalog cost (matched by itemId or name). The shop view gives each posse member `sell`; the Store's Their Inventory lists it. Sell requests carry `key`; approval runs `removeSold`, which also takes a sold inventory item off its sheet section (`unequipItem`) and blanks sold sheet slots (name re-checked first).
- Catalog:
  - `lib/catalog.js` holds 307 items, generated from the price list. Item ids look like `pistols-used-pistol`.
  - `img` names a file in `/img/store/<img>.webp`.
- Factions:
  - `lib/npcs.js` `allFactions(state)` returns the book's 7 plus `state.factions` (custom, with a `known` flag).
  - Players only see known factions. `GET /api/npcs?view=factions` returns them.
- Tokens: `/img/tokens/{trade-*, monster-*, npc-*}.webp`, chosen by `artFor()` in `lib/battle.js`.
- Shared client UI (use these, don't reinvent):
  - **Icons:** no emoji anywhere (user dislikes them). `gl(name)` from `public/js/glyphs.js` returns an inline SVG line icon (revolver, boot, dodge, star, forstall, satchel, bandage, lasso, watch, heart, skull, drop, flash, claws, horseshoe, bullet, scroll, die, trophy, fire, target, lock, wrench, hat, pin). Static HTML uses `<span data-gl="name"></span>`, drawn by `mountNav`. Plain-text spots (toasts, `<option>`, titles) get no icon; ✓ ✗ → ← are fine. Old log entries have emoji stripped at render (`noEmoji` in tablelog.js).
  - **Dialogs:** never `confirm`/`prompt`/`alert`. `await ask(msg)` → true/false and `await askText(msg, default)` → string|null (common.js). A short leading question becomes the heading; destructive wording gets a red button.
  - **Every page** (`mountTableLog`): Table Log + Roll dice buttons share `.fab-row` (bottom-right; stacked on phones); `mountDice` rolls any B/G pool as anyone; first visit asks "Who are you playing?" (`askWhoIAm`, sets `wiw.me`, `wiw.meAsked`), skipped in Warden mode.
  - `/howto` = How to Play Online (static guide for players, in the nav). Update it when player-facing flows change.

- The old Session page is merged into Run the Game: `/session` redirects to `/run` (vercel.json + a tiny session.html). Session notes, open rolls, homebrew, backup and the recent log live in `public/js/desk.js` (+ css/desk.css), mounted by run.js via `mountDesk`. Keep Warden pages in Run the Game's style (shared `.stack-grid`, `.item-row`, `.notice`, `.link-grid`). The page is grouped into bands (`.band`: Right Now, Rolls, At the Table, Rewards — award / Jackpot / Town Rest, moved from the Posse page —, Notes & Log, Tools — Jump To + Backup | Homebrew) with a sticky contents bar `.toc-bar#run-toc` (top = `--toc-top`, set by `tocTop()` in run.js).
- Session write-up: every log() also appends to `combat.archive` (Warden-only, 2000 entries, undo trims it; never in views) because the Table Log keeps only 80. Session notes' **Write up this session** → `{action:'summarize', id}` in lib/routes/session.js: `sessionEntries` (from the session's `created` to the next session), then Claude (`ANTHROPIC_API_KEY` env on Vercel; model `SUMMARY_MODEL` or claude-haiku-4-5) writes STORY SO FAR / FIGHTS / LOOT / PEOPLE / LOOSE ENDS + a player RECAP (fills an empty recap); without a key `plainSummary` lists the events. `withSummary` puts it between `=== SUMMARY ===` and `=== MY NOTES ===`, keeping the Warden's notes below; re-running replaces only the summary. vercel.json gives the API function 30 s (the AI write-up needs it).
- Session notes: `lib/routes/session.js` + `lib/session.js` (Redis key `session`), every request needs the PIN; shown on Run the Game.

## Pictures
- `lib/routes/image.js` stores uploaded pictures as base64 under `img-<ns>-<id>-<head|full>` (ns `pc` = portraits, anyone; `handout` = Warden only) and serves `GET /api/image?ns&id&size&v` with immutable caching. Uploading/clearing a pc portrait sets/removes `pc.portrait = {v}` in the combat doc.
- `public/js/portrait.js`: `faceUrl(p)` (headshot or trade art), `portraitUrl(p, size)`, `pickPortrait(pc)` (file → in-page cropper, 256px round headshot + ≤1200px full), `clearPortrait`, `showImage(src, caption)` lightbox, `shrink(img)`. Used on the Posse list, sheet header (`faceHTML`, refreshed in `hydrate` when `portrait.v` changes), Battle Map tokens (`photo` in the battle view) and Run the Game rows.

## Handouts & Backpack
- `lib/handouts.js` + `lib/routes/handouts.js` (doc `handouts`): `{id, kind item|note, title, text, img(v), to 'all'|[pcIds], shared, sharedBy, seen[]}`. Only the Warden sends/edits/removes; a recipient can `share` ("Show the posse"); `seen` per character. Players GET `?view=player&pc=<id>` (only what they may see); Warden `?view=warden`. Handout items are story props — never on sheets. Photos via `lib/routes/image.js` ns `handout` (Warden), which sets `h.img`.
- Warden: **Hand Out** card on Run the Game (Rewards & Handouts band; `public/js/handout-send.js`). Players: `watchHandouts()` (public/js/handouts.js, started by `mountTableLog`) pops up unseen handouts on any page ("The Warden hands you…", Show the posse / Open the Backpack / Keep it). `/backpack` (World ▾) lists shared handouts + "Just you"; the Warden sees all with Make private / Delete. Notes use `--hand` (Reenie Beanie) on torn paper (`.note-paper`).

## Whispers
- `lib/whispers.js` + `lib/routes/whispers.js` (doc `whispers`): player `send {pc, text}` → Warden pop-up on any page (`watchWhispers` in public/js/whisper.js, started by `mountTableLog`; also a Needs-you item) with Later / Got it / Reply (`reply` one line). Warden `wardenSend {to: [pcIds]|'all', text}` (Run the Game **Whisper** card, `mountWardenWhisper`) → the player's pop-up "The Warden whispers…" with Reply. Players only ever receive their own unseen replies (`replySeen`). No chat history. The **Whisper** button sits in the fab row for players.

## Lock picking
- `lib/lockpick.js` + `lib/routes/lockpick.js` (doc `locks`): High/Low with a server-side 52-card deck (A = 14). Warden `start {to, difficulty 1–5 = wins in a row, retries, retryCost, what}` → per player status `finesse` → pcOp-like `finesse` (rolls the sheet's Finesse; Poisoned −2, Finesse Talent spurs; each Hit = one `peek` at the next card's color) → deal (`ace` status if the starter is an Ace: player calls high/low) → `guess {dir}` (strictly higher/lower wins, **ties lose**; an Ace drawn next is always high) → `picked` after `need` wins, `failed` on a miss → `retry` (pays the Warden's cost, fresh deck, `retriesLeft`) / `giveUp`. Players only ever see their own attempt, never the deck (`lockView`). Results go to the Table Log. UI (`public/js/lockpick.js`): Warden **Lock Pick** card on Run the Game (Rolls band: who, what lock, difficulty chips, retries + cost, recent attempts); players get the full-screen brass-padlock scene on any page (`watchLocks`, started by `mountTableLog`) — Finesse roll, Ace call, Higher/Lower, Peek, pins rise per win, shackle opens / pick snaps, Try again (confirm the cost) / Walk away. Card classes are `.lk-card`/`.lk-back` (the NPC page owns `.pcard`/`.back`).
- **Lockpicks:** a house item in the catalog (`general-goods-tools-lockpick`, $0.50, `house: true`, shown as "House item" in the Store). A retry whose cost mentions a lockpick takes one out of the picker's inventory (`lib/routes/lockpick.js`), or refuses if they have none. **Locksmith** (Trapper, 2/day) adds +2B to the Finesse peek roll and uses one of its daily uses.
- **Lock scene extras:** every lock in the API view carries `picks` (lockpicks in the picker's Inventory, summed across stacks) and the scene shows it in the header; a CARDS PLAYED row shows the starter card and every card drawn this try, with each call (▲/▼). Give up / Walk away / Done mark the lock `closed` on the server and the device remembers it locally so a poll in flight can't reopen it; the Warden's Lock Pick list hides closed locks (the Table Log keeps the result).
- **Loot & traps:** `start` also takes `loot {kind: money|scrap|item|custom, amount | itemId | name+desc}` and `trap {damage, status, sev}` (hurts whoever opens it). Both stay hidden from players (`lockView`) until `picked`; then `lockAction` calls `onOpen(a, 'trap'|'loot')` once each, and `lib/routes/lockpick.js` applies them to the picker automatically — trap via `publicAction` health/status, Money → `wallet`, Scrap → `scrap`, store item → inventory + `equipItem`, new item → inventory (`sub: 'Found'`, `note` = description). `a.sprung` / `a.found` carry the result text for the scene and Table Log.

## Sound
- **Real recordings** (free Pixabay sounds the user picked, in `public/sfx/`): `CLIPS` in sound.js maps a sound to `[file, start, length]` clips, one picked at random: dice, shuffle (4 shuffles in one file), card (12 single snaps from deal.mp3), chips, drink (pour + slam), gun (pistol/pistol2/rifle), shotgun (`weaponSound` sends shotguns there). Buffers load on the first tap (`loadClips`); until then, and for anything without clips, the synth plays (`FALLBACK` for the new names). Saloon: a new deal shuffles (dice for Liar's Dice), bets/calls/raises and faro/blackjack stakes click chips, a drink pours.
- `public/js/sound.js`: `play(name, arg)` synthesizes effects with WebAudio (dice, card, gun, bow, swing, explosion, forstall, zap, lockClick, lockSnap, lockOpen, success, fail, chime); `weaponSound(weapon)` picks gun/bow/swing. Real recordings: put `public/sfx/<name>.mp3` and add the name to `public/sfx/manifest.json`. Mute/volume per device (`wiw.muted`, `wiw.volume`) via the nav speaker / phone Menu. Hooks: `animateRoll` (dice), NPC `flipUp` (card), Battle Map attacks/items/Forstalls, HUD pop-ups (chime), Skill results (success/fail).

## Style
- **Read `STYLEGUIDE.md` before any UI change.** Tokens (colors, type scale, spacing, corners, shadows) and shared components (`.head-row`, `.stack-grid`, `.band`, `.toc-bar`, `.chip-btn`, `.field-step`, `.pill`, `.item-row`, `.notice`, `.link-grid`, buttons) live in `public/css/style.css`; page CSS holds only page-specific rules. No raw hex colors in page CSS.

## Conventions
- **Cache busting:** bump `?v=N` on the page's css/js tags after edits. Pages import `common.js` unversioned, so bump the page scripts when common.js changes.
- **Edits with Python:** write the script to a file in the scratchpad, then run it. Bash heredocs with quotes and backticks break.
- **Local preview:** config "wiw-forstall" on port 5190, in `mat-headz/.claude/launch.json`. Restart it after any `lib/` change, because the dev server caches modules.
- **Verify live** by polling `curl https://wiw-forstall.vercel.app/<page>` until the new `?v=` shows up.
- **Rule checks:** `npm test` (node:test, `tests/rules.test.js`: Scanner scoring, ranges, Sweep/Tolerance/Edison, move cost, Store→sheet, upgrades, a combat Sweep). Run it before every push and add a check when you add a rule.
- **After each chunk:** commit and push to `main`. Vercel auto-deploys.

## User preferences
- Work in small chunks and push live after each one.
- Keep things simple. The dice animation reuses the existing tray; don't build new 3D.
- NPC names toggle is Male / Female / Random.
- Warden approves store purchases.
- Spur boxes link to Talents.
- No emoji: use the line icons. No browser pop-ups: use the styled dialogs.
- Player-facing copy should make sense to a player (no pointers to Warden-only pages).
- The live data is real (the group's characters and NPCs). Test only locally; never write to or clean up live data.

## End Session
`public/js/endsession.js` `openEndSession` — the **End Session** button at the end of Run the Game's contents bar opens a 7-step walkthrough: Loose ends (end the fight, close open rolls, call off unfinished lock picks) → Prestige (per character, "everyone gets N", one `award` per amount) → Jackpot (p. 54) → Titles (grant Achievements, p. 34) → Rest (Town Rest for all, or out on the trail) → Write-up (AI `summarize` + the Warden's own notes under MY NOTES + a recap, optionally posted to the Table Log) → Wrap (download a backup, then session `end`, which logs the send-off). Tonight's session is the newest un-ended one from the last 20 hours; otherwise one is created with `created` reaching back to the last session's end (max 14 h) so the write-up covers the whole night. The Guidebook's own end-of-session procedure was not available when this was built — steps come from the rules the app already uses.

## Duel requests (p. 58)
- Players tap **Challenge to a Duel** on a known NPC in the ledger (`challenge()` in names.js) → combat action `duelRequest {pc, npcId, npcName, reason}` → `state.duelRequests` (one pending per character, last 20). Warden `duelAnswer {id, accept, tough: Weak|Moderate|Strong, note}`: accept calls `duelStart` with `pc:<id>` vs `np:npc:Human - <tough> Combatant|<name>` (p. 191); deny keeps the note. Pending ones are Needs you items. `duelStart/Roll/End` now need the PIN.
- `public/js/duel-hud.js` `duelHud(h)` runs from `renderHud` (every page but /combat), fed by `logView().hud.duel` + `hud.duelReqs`: the Warden's challenge card (toughness chips, Later / Say no / Accept), the player's "No Duel" answer (seen ids in `wiw.duelSeen`), and the Duel viewer for everyone (`.duel-scene`; the Warden rolls each step and closes it; Hide is per tab).

## Wanted posters
- `lib/wanted.js` + `lib/routes/wanted.js` (doc `wanted`: `posters`, `towns`). Poster `{id, town, name, alias, crime, reward, terms, npcId, pcId, img(v), status wanted|captured|dead|claimed, claimedBy, paid, wardenNote, hidden}`. Towns = `allTowns`: Guidebook towns/settlements/rail towns (`BOOK_TOWNS` from places.js) + the Map's Warden pins + towns made on the Wanted page (`wt-*`). Every action is Warden-only; `payout {id, to:[pcIds], bonus:[pcIds]}` splits the reward into wallets (+20% for Cut of the Profit, a Gunslinger ability, p. 20) and marks it claimed. Posting a visible poster (or un-hiding one) logs it to the Table Log.
- GET adds `here` (town nearest the middle of the posse's Map tokens, within ~88px), `photo` (uploaded `api/image` ns `wanted` → the character's portrait → the ledger NPC's token art), and for the Warden `posse` (with `cut`) + `npcs`. Version = `${wanted.v}.${map.v}`.
- **Taking a bounty:** player action `take {id, pc}` (the only non-Warden action): adds the pc to `poster.takenBy`; the first taker makes a revealed Journal quest via `bountyQuest` (lib/journal.js: "Bounty: <name>", 3 steps, `quest.bounty` = poster id, `poster.quest`); later takers just join. `bountySync` keeps it in step: captured/dead ticks steps 1–2, payout ticks all and marks it done, deleting the poster fails an open quest. lib/routes/wanted.js loads the `journal` doc and saves it only when quests changed; views carry `names` (pc id → name) for "On the trail". UI: Take the bounty in the big poster view (players, needs "This is me"), "taken by" tag on the board.
- `/wanted` (World ▾; public/wanted.html, js/wanted.js, css/wanted.css): town chips (posse's town first, "Another town…" picker), a plank board of tilted paper posters (Rye WANTED, sepia likeness, typewriter crime, red stamp CAPTURED/DEAD/BOUNTY PAID). Warden mode on the same page: New poster / New town, and in the big view Status chips, Pay out, Edit, Hide, Delete. The Map's place panel links "N wanted in <town>" to `/wanted#<id>`.

## Newspaper recap
- `lib/papers.js` + `lib/routes/papers.js` (doc `papers`: `issues`). Issue `{id, no, sessionId, paper, town, townName, date, headline, subhead, lead, stories[{head,text}], quote, ads[], classifieds[{name,reward,crime}], faces[pcIds], published}`. `paperName(town)` = "The <Town> Gazette/Clarion/…" (stable per town). Warden `draft {sessionId, town}` builds a front page from the session's archive entries **minus hidden ones** (Claude JSON in an 1880s voice when `ANTHROPIC_API_KEY` is set; else `plainFrontPage` from the recap + moments), adds that town's live Wanted posters as classifieds and the posse members named in it as `faces`; one unpublished draft per session is reused. `save`, `publish` (logs "Extra! Extra! …"), `unpublish`, `remove`. Players see published issues only; views add `posse` for portraits.
- `public/js/paper.js`: `paperHTML` (blackletter masthead UnifrakturMaguntia, Rye headline, Old Standard TT body, 3 ruled columns → 1 on phones, halftone sepia faces, WANTED box, ads), `openPaper` (reader with Print; print CSS shows only the paper), `watchPapers` (players: newest unseen issue pops up once per device, `wiw.papersSeen`; started by `mountTableLog`). Styles + fonts load on demand (`paperStyles`, css/paper.css).
- End Session step 7 **Front page** (town picker defaulting to where the posse is; Set the type → edit headline/stories/quote with a live preview → "Print it & next" publishes). Archive: `/journal` (World ▾) Newspapers section; the Warden can print/take back/delete there.

## Journal (quests & clues)
- `lib/journal.js` + `lib/routes/journal.js` (doc `journal`: `quests`, `clues`, `news`). Quest `{id, title, text, giver (npc id), where (town id), reward, steps[{id,text,done,hidden}], status open|done|failed, revealed, posseNotes}`; clue `{id, title, text, img(v, api/image ns clue), npcs[], places[], quest, revealed, posseNotes}`. Warden: `saveQuest`, `step {step, done|hidden}`, `status`, `saveClue`, `reveal {kind, id, value}`, `remove`; anyone: `posseNote {kind, id, text}` on something revealed. Reveals, showing a hidden step, ticking a visible step and finishing a quest push `news` (last 40). Player view: revealed only, hidden steps stripped, a clue's quest link blanked if that quest is hidden. GET adds `npcs` (known ones for players) + `towns` for link names.
- `/journal` (World ▾): tabs Quests | Clues | Newspapers (`#quests/#clues/#papers`). Quests = cards with step checklist (the Warden ticks/hides steps inline), reward, linked clues, handwritten posse notes (autosave). Clues = corkboard of tilted pinned notes, filter by quest/NPC/town, tap for the big note. Warden forms (New quest / New clue, steps editor, picture upload) on the same page. `public/js/journal-watch.js` `watchJournal()` (players, via `mountTableLog`, not on /journal) pops up news newer than `wiw.journalSeenAt` (first visit just sets it; opening the Journal marks everything seen).

## Scene prep
- `lib/scenes.js` + `lib/routes/scenes.js` (doc `scenes`, **Warden-only**, never in player views): `{scenes, current}`. Scene `{id, title, town, readAloud, notes, npcs[], wanted[], journal[{kind,id}], battleMap (preset id), enemies[{profile,name,count}], handouts[{id,kind,title,text}], locks[{id,what,difficulty,retries,retryCost,loot,trap}], checks[{id,skill,diff,note}], used{beatKey: at}, done}`; actions `save {id?, scene}` (`tidy` clamps everything), `copy`, `remove`, `current {id}`, `used {id, key}`, `done`, `reset`.
- `/prep` (Warden nav **Prep**; public/prep.html, js/prep.js, css/prep.css): scene list + editor (chips for NPCs / hidden posters / quests & clues, battle-map chips, enemy/handout/lock/roll rows). Save, Copy, Delete, Run it tonight.
- Run the Game → **Tonight's Scene** band (`public/js/scene-run.js` `mountSceneRun`): pick the scene, read-aloud box, Warden notes, and one button per beat. Beats call the existing endpoints: Set the stage = npcs `known` + wanted `edit hidden:false` + journal `reveal`; Set up the fight = battle `preset` + combat `addEnemy` (combat itself is still started by the Warden); handouts = handouts `send` to all; locks and rolls ask who (`pickWho`) then lockpick `start` / combat `checkStart`. Used beats get a ✓ (doing one again asks first); Scene's over marks it done.

## Saloon: poker (five-card draw)
- `lib/saloon.js` + `lib/routes/saloon.js` (doc `saloon`: one `table` at a time). Warden `open {where, ante, bet, npcs[{name, profile, style tight|loose|bluffer, bank}], invite[], tell, bluff, palm}` → players `join {pc}` → `deal` (Warden or a seated player) → antes, 5 cards each from a server deck (`newDeck` from lockpick.js, A = 14) → `move {pc, move: check|bet|call|raise|fold}` (fixed limit: `bet` in round 1, double in round 2, 3 raises a round; can't call without the money) → `draw {pc, discard[]}` (up to 3, or 4 while keeping an Ace) → round 2 → showdown (`rankHand`/`compareHands`, split pots). NPC turns resolve server-side right after each action (`npcBet`: hand strength + style + `ctx.rand`; `npcDiscards`). Money moves straight between wallets and NPC `bank`s; `seat.net` tracks winnings; `close` refunds an unfinished hand and logs the tally.
- Skill moves (house rules on p. 12–13's Challenge Roll, ties reroll; rolls go to the Table Log as "Poker · Skill"): `tell {target}` Intuition vs Charm → one NPC card in `hand.peeks`; `bluff` (on your bet) Charm vs each NPC's Intuition → beaten NPCs without two pair play scared (`seat.bluffed`); `palm {card}` (on your draw) Finesse vs the sharpest NPC's Intuition → swap in the better of two deck cards, or get caught: folded out and logged. Each once per hand.
- `saloonView`: players see their own cards, earned peeks, what they owe, and every hand at the showdown; the Warden sees all hands (`hand.all`) and NPC styles/banks.
- UI: `public/js/saloon.js` + css/saloon.css (loaded on demand by `saloonStyles`). Warden: **The Saloon** card on Run the Game → At the Table (`mountSaloonDesk`: where, stakes, up to 4 NPCs — name or pick from the ledger, Green/Seasoned/Sharp = the p. 191 Weak/Moderate/Strong human profiles, style, bank — invites, Skill-move chips; once open: seats + every hand, Watch the table, Deal, Remove, Close). Players: `watchSaloon()` (via `mountTableLog`) asks once per table (`wiw.saloonAsked`) → join → the full-screen felt table (`openTable`): seats, pot chips, your fanned hand (tap to pick discards on the draw), Check/Bet/Call/Raise/Fold, Bluff, Palm (with exactly one card picked), Read a tell per NPC; your own Challenge roll pops up (`showRoll` reads it from the Table Log). The table opens itself when it becomes your move; × steps away and a "Back to the card table" chip (`.sl-chip`) stays in the corner.

## Saloon: faro
- `lib/faro.js` (used by lib/saloon.js when `table.game === 'faro'`; `open {game:'faro', crooked}` keeps one NPC = the dealer/bank). `deal` = a fresh deal (`newFaroDeal`: soda card shown, `faro.case` counts ranks out). `faroBet {pc, rank 2–14, amount (0 = take back), copper}` (stake leaves the wallet while on the layout; min = ante, max = 5 × bet; dead ranks refused). `faroTurn` (any seated player or the Warden): banker's card loses / player's card wins even money (stake stays down), coppered bets reverse, a pair = split (bank takes half), the last turn resolves `faroCall {order[3], amount}` (4:1, cat-hop 2:1) and the hock, then returns every stake and logs the tally. Crooked box (Warden secret, `t.crooked`): 30% of turns the rank with the biggest straight exposure is slid on top as the banker's card (`faro.cheats`). `faroWatch` Intuition vs the dealer's Finesse, once per deal: catches a crooked box (turns it off, logs it). `faroRefund` on leave/kick/close. View: `table.faro` (bets are public chips; calls only your own; `crooked`/`cheats` Warden-only).
- UI: `renderFaro` in public/js/saloon.js — soda / bank's card / player's card / hock, the 13-card spade layout (tap to bet: `betDialog` amount ± and Straight/Coppered, Take it back), other players' chips as dots, the casekeeper beads, Deal the turn, Call the turn (`callDialog`, when 3 are left), Watch the dealer. Warden Saloon card has Poker/Faro chips and the Square/Crooked box choice.

## Saloon: Liar's Dice (classic six-sided, the user's pick)
- `lib/liars.js` (used by lib/saloon.js when `table.game === 'liars'`; up to 4 NPCs with styles). `deal` = a new game (`newLiarsGame`: everyone who can pay antes into the pot, 5 dice each, starting seat rotates). `liarsBid {pc, qty, face 2–6}` (ones are wild; must be more dice, or the same number of a higher face — `legalRaise`), `liarsCall {pc}` → `lift`: count face + ones across every cup; fewer than bid = bidder loses a die, else caller; `last` keeps the reveal; loser starts the next round (all reroll); last seat with dice takes the pot and it's logged. NPC turns (`npcTurn`): call when the bid is past what they'd expect (own matches + others/3) by their style's edge, else raise on their best face; bluffers sometimes push. Skill moves (`hooks.peek`, `hooks.stare`, once a round): `liarsPeek {target}` Intuition vs Finesse → one of their dice in `peeks`; `liarsStare` (on your turn) Charm vs the next NPC's Intuition → `stared`: that NPC raises instead of calling. Leaving mid-game with dice is refused; kick drops their dice (or refunds if only two left); close refunds the pot evenly (`liarsRefund`). Dice are drawn with CSS pips (`.ld-die`, wild ones in rust), `renderLiars` in public/js/saloon.js.

## Saloon: Skill rolls, rules panel, fixes (2026-09-25)
- Skill-move rolls use `poolFor` in lib/routes/saloon.js: the sheet's Skill with **Poisoned −2 dice** (gold kept last), Talent Spurs; labelled `<Game> · <Skill>` in the Table Log. POST responses carry `rolls` (every die rolled for that action) so the client pops up the player's own roll with both scores (`showRoll`). Views are `decorate`d with each NPC's Charm/Finesse/Intuition pools and `table.me` (your pools after Poisoned).
- **Rules & key** button on every table header (`openRules`): How to play (`RULES`) and Skill moves (`MOVES` → `keyHTML`: what you roll vs whose Skill with the real pools, when, win, lose; switched-off moves greyed). Move buttons show the matchup too.
- Layering: `.ask-back` is z 450 (confirmations above every game screen — lock, duel, saloon 225, End Session) and `.toast` z 600. Before this, a hidden confirm froze the saloon (`busy` never cleared). × and Rules now work even while an action is running. Liar's Dice players out of dice get a Leave button.

## Saloon: blackjack
- `lib/blackjack.js` (used by lib/saloon.js when `table.game === 'blackjack'`; one NPC = the dealer, who banks). Two-deck shoe kept across rounds until under 20 cards. `deal` → `newBjRound` (phase `bets`) → `bjBet {pc, amount}` (min = ante, max = 5 × bet; 0 takes it back; the stake leaves the wallet; when every seated player has a bet the cards come out) or `bjDeal` (any seated player or the Warden, once someone has bet) → phase `play`: two cards each, dealer one up + hole card; player blackjacks stand automatically; dealer peeks under an Ace/ten-card → `bjMove {move: hit|stand|double}` in seat order (double = first two cards only, one card) → dealer draws to 16, stands on all 17s (skips drawing if nobody is left standing) → settle: blackjack 3:2, win even money, push returns the bet → phase `done`, `results`, logged. Skill moves (`hooks.count`, `hooks.shiner`, once a round): `bjCount` Intuition vs the dealer's Finesse → the next card's band (high/low/middling; cleared on the next draw); `bjShiner` (your turn) Finesse vs the dealer's Intuition → see the hole card (`holes`), caught = bet forfeit + out of the round + logged. Leaving mid-hand is refused; `bjRefund` on leave (bets phase)/kick/close; `bjResume` carries on after a kick.
- UI: `renderBj` in public/js/saloon.js (dealer row, "Dealer draws to 16 and stands on all 17s", a hand per player, bet stepper, Hit / Stand / Double down, skill buttons with matchups). `turnOf(t)` (poker/blackjack/Liar's Dice) opens the table and lights the chip on your move.

## Run the Game layout
- Each band's `.stack-grid` columns are balanced by hand in run.html: Rolls = Call for a Roll + Open Rolls | Lock Pick; At the Table = Posse + Enemies | The Saloon; Rewards = Award + Jackpot + Town Rest | Hand Out + Whisper. Keep them even when adding a card (no auto-balancer: live content would make cards jump columns).
- The Saloon card's NPC rows are one `.field-step` per NPC (name input + ledger select, Green/Seasoned/Sharp chips, style chips, bank), per STYLEGUIDE.md.

## Trading between players
- `lib/trade.js` `tradeAction` (combat doc `state.trades`, last 30), called by lib/routes/combat.js for `{action:'trade', op}` (no PIN; players act as `pc`). `offer {pc, to, give:{money, things[{key,qty}]}, get:{…}, note}` — things are `sellables` keys (lib/shop.js: inventory rows + starting weapons/gear/horse/mech/Forstall), max 3 pending per player. `answer {pc, id, accept}` (recipient only) runs both directions on `structuredClone`s and keeps them only if everything worked, else `status:'failed'` + `why` (money short, no free weapon slot, already has a horse, item gone). `take` moves a thing's sheet section with it (weapon slot with upgrades/ammo, whole Forstall/horse/mech, gear slot when the last one leaves; ammo/upgrades via unequipItem → equipItem); `give` stacks Gear/Goods/Traps rows. Accepted trades are logged. `cancel` (offerer only).
- `logView().hud.trades` (pending + answered in the last day) → `tradeHud` in public/js/trade.js from `renderHud`: the recipient's offer pop-up (Later per tab / No thanks / Deal), a one-time toast for the offerer's answered offers (`wiw.tradeSeen`). `openTrade(withId)` dialog: partner chips, YOU GIVE / YOU WANT (money ± and a chip per thing; taps on a stack add one more), note, your open offers with Take it back. Entry points: the players' **Trade** fab (tablelog.js, next to Whisper) and any `[data-trade]` button (the sheet's Inventory: "Trade with the posse").

## Posse stash
- `stashAction` in lib/trade.js (reuses `side`/`take`/`give`), combat doc `state.stash = {money, items[{id, name, qty, where, packet}]}` (a packet = the inventory entry + its sheet sections, so a gun keeps upgrades, a horse its Bond). `{action:'stash', op:'put', pc, money, things[{key,qty}]}` / `op:'take', pc, money, id, qty` (runs on a structuredClone of the character; partial stacks split the entry, one weapon section per unit) / Warden `op:'loot', money, itemId, qty` (catalog + homebrew via the shop). Stackables merge. Every move is logged. `playerCombatView` carries `stash` (no packets).
- UI: `openStash()` in public/js/stash.js (`stashSummary` for cards). Any `[data-stash]` button opens it (tablelog.js): the **/stash** page (World ▾ → Posse Stash; public/stash.html + js/stash-page.js lists the contents, refreshed every 5 s), the Posse list's **Posse Stash** card, the sheet Inventory's "Posse stash" button, and Run the Game → Rewards (right column) for the Warden's loot form.

## New-player tour
- `public/js/tour.js` `runTour(steps, key, {force})`: dims the page (`.tour-back`, z 500), spotlights one element (`.tour-spot`, box-shadow cut-out; `up` = highlight the nearest ancestor), caption bubble with Skip / Back / Next (docked to the bottom under 600px). Steps whose element is missing or hidden are skipped. Done/skipped sets `wiw.tour.<key>`; never runs in Warden mode. `SHEET_TOUR` (This is me star → Health & Grit → Skills and how a roll works → Weapons → Inventory/Trade/Stash → the fab row → the nav) runs from posse.js `render()` the first time a player opens their own finished sheet. Replay: How to Play's "Take the tour of your sheet" → `/posse?tour=1` (jumps to your sheet and forces it). Keep the steps' selectors in step with the sheet markup.

## Saloon: drinking contest (house rules)
- `lib/drinking.js` (used by lib/saloon.js when `table.game === 'drinking'`; up to 4 NPC drinkers, toughness only). `deal` → `newContest` (everyone who can pay the entry antes into `pot`; NPCs get Grit Weak 2 / Moderate 4 / Strong 6 and their profile Health). `drinkPour` (any seated player or the Warden, phase `ready`) → round N: NPCs drink at once; players `drink {pc, grit 0–3}` (Grit comes off the sheet). A shot = Nerve (`ctx.poolOf`: sheet pool with Poisoned) − Drunk levels − a needle + Grit as Black dice (`shotPool`, Black removed first), needs Hits ≥ round. Miss → Drunk +1 and 1 Health (never below 1; hitting 1 passes you out); 3 Drunk = out, `ctx.status(pc, 'Dazed', 1)`. When everyone upright has drunk, the round ends; ≤1 upright → the last one takes the pot (or those who went down in the final round split it), the first one out pays the tab ($0.10 × shots poured), logged. Skill moves (`hooks.spittoon`, `hooks.needle`, once a contest): `drinkSpit` Finesse vs the sharpest upright NPC's Intuition → skip the shot, caught = a double (two rolls); `drinkNeedle {target}` Charm vs that NPC's Nerve → −2 dice on their next shot. Leaving while upright is refused; kick/close refund the pot evenly (`drinkRefund`). lib/routes/saloon.js ctx adds `poolOf`, `npcHealth`, `status`, and `roll` accepts an exact `{black, gold}` pool; `table.me` now carries `grit`/`health`.
- UI: `renderDrinking` in public/js/saloon.js (bar with a seat per drinker: three shot glasses that tip as they get Drunk, Health, last shot's Hits; Grit stepper; Drink shows the dice math; Pour a round; spittoon/needle with matchups). `turnOf` opens the table when your glass is poured.

## Fighter cards on the Battle Map (replacing Combat Control, step 1)
- `public/js/fighter-card.js` (styles `css/fighter.css`, moved out of combat.css): `pcCardHTML` / `enemyCardHTML` / `wireFighters(box, {data, meta, act})`. Health ± (or type −5 / 3), Grit pips, Statuses ± and add, Aces, Ace-in-the-Hole, Bleeding Out, Revive; enemies add the stat block (Defense, Skills, attacks, status pools as tap-to-roll buttons), frenzy rows, features & tolerances, rename, remove, and loot/search when downed. **Every roll is public** (`{action:'roll', who, pool, label}` → Table Log + `rollPopup`) — the user wants no secret rolls. Same server actions Combat Control used (`pc`/`enemy` ops, `loot`, `search`).
- The Battle Map shows the card in the side panel when the Warden taps a token that's in the fight (battle.js `renderPanel`; the plain summary rows hide when a card shows). In Warden mode the map's side panel is 440px (the Warden runs it from a desktop). The panel doesn't redraw while the Warden is typing in it.
- **Add enemies** (`public/js/enemy-add.js` `openAddEnemies(combat, done)`): a dialog from the map's fight bar ("Add enemies" before a fight, "+ Enemies" during one) with Monster / Person / Custom tabs — the same `addEnemy` actions Combat Control used (ledger NPCs pick Weak/Moderate/Strong to fight like, random names from npc-data.js); each add also runs battle `syncCombat` so the token lands on the map, optionally hidden (ambush).
- **Spoils** (`openSpoils` in fighter-card.js): End combat on the map opens a pop-up listing the downed/fled enemies with the loot box (Give trophy → the sheet's Inventory text, Search the body (Intuition) → public roll). Downed enemies can also be looted any time from their fighter card.
- Plan (user-approved direction, 2026-09-25): Combat Control's jobs move onto the Battle Map for the Warden, then `/combat` retires. Next: an Add-enemies dialog from the map's fight bar, a Spoils pop-up when combat ends (loot the downed), then redirect `/combat` → `/battle` and drop it from the Fight menu. Surprise stays only as the Start-combat option (p. 40: surprisers get the first turn).
